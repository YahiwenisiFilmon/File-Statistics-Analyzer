# File-Statistics-Analyzer
A CPP File System &amp; Log Analyzer is a CLI tool that scans directories and analyzes log files to produce detailed statistics and insights. It reports file sizes, types, age, and storage usage, and parses common log formats to extract metrics, trends, and errors. Supports filtering, aggregation, and text/JSON output.
